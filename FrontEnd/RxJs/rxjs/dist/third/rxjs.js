"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 *   combineLatest
 */
const rxjs_1 = require("rxjs");
let boys = rxjs_1.Observable.from(["tom", "jhon", "jack"]);
let girls = rxjs_1.Observable.from(["annie", "amanda", "jane"]);
rxjs_1.Observable.combineLatest(boys, girls, resultSelector, {}(boy, String, girl, String) in
);
return (boy, girl);
subscribe;
{
    event in
        print(event);
}
disposed(by, disposeBag);
